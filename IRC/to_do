Claire 🎅🎄 : 

admin
die
nick
oper
pass
ping
quit
squit
user

Amandine 🎈🍒 :

invite
join
kick
list
mode 1 et 2
names
part
privmsg
time
topic





------------------------------------------------------------------------------------
WITH CLAIRE


MODE +i bonne reception, cependant voir usage
MODE +o (le reste) bonne reception mais mauvais msg de irssi , voir usage

PING revoir les numeric replies

voir les differences et les usages des operator (channops, ircoperator, serveroperator)

JOIN faire la recpetion de MODE #channel + WHO #channel
    RPL  RPL_TOPICWHOTIME (333) 
    pas d'arobase pour l'un des deux qui envoie un message dans le channel
    topic for #toto: No topic is set : toutes les 5min la notif est envoyé... solution whotime peut-etre ?

MSG pas d'arobase ?
    si plusieurs user : a verif car retour nop : si envoie a soi-meme, refuser : usergroup
    /msg toto, toto, toto, uu
    
Laisser le serveur jouer tout seul

TOPIC ne marche plus

Quit command, quand irssi quitte, aller dans initserver Ligne 83, check leak 

SQUIT verfier aucun leak de open + leak tout court (use, channel)

PART ne focntionne pas

ADMIN : RPL_ADMINLOC2 (258) 

TIME : timestamp à mettre dans le result directement

------------------------------------------------------------------------------------








✔️list
✔️names
✔️admin
✔️nick
✔️oper
✔️pass
✔️user
✔️quit
✔️ping
✔️
✔️


〰️topic : ne change pas, bug ne reconnait pas le user, doit recommencer deux fois, le supp on dirait

〰️join : doit etre taper 2 fois pour rentrer (que pour la 1ere fois) => bug pour autre utilisateur

〰️privmsg : /msg #r d channel non trouver n'affiche pas l'erreur mais affiche sur la console, nick non trouver affcihe erreur mais pas sur la console
            - pas de arobase pour l'autre recevru de chan
            - l'autre arobase meme dans le "channel name" du user
            - en msg priver si ecrit en perso : "tt Cannot send to channel"


pass not mandatory si pas rentrer au debut du programme

〰️ part #cc : pass dans ka fonction mais dans la window ne se supp pas

〰️ mode #toto :
    -  mode/#toto [visible public -i], pas sur pour l'affichage du mode/  [invisble] .. consequence ?
    -  devient channops OK, juste pas de message pour le client, normal ?

〰️ invite : le message s'affiche coté serveur, "notif [] dans irssi MAIS mais rien coté irssi, cependant reste fonctionnel

〰️ die, squit : ERR_NOPRIVILEGES generale ServerOPR or IRCOoperator + exit() + erase users, channel ?

repertorier tous les if des channops etc pour regler le tout qui doit faire quoi qui peut quoi (dont tout mode user)

faire des tests avec plusierus channels
❌ kill


❌ who, whois, whowas ??

//not mandatory
❌ AWAY
❌ Users
❌ WALLOPS

⚠️ ⚠️ ⚠️ ⚠️ ⚠️ userhost != user !! ⚠️ ⚠️ ⚠️ ⚠️ 

pas de diff pour l'instant entre ircop et server op, checker qui a besoin d'etre channops, =>  != /oper  

server name a mettre en getter steer, et bcp d'autres aussi

voir tous les if return if return par if else ifonly (pour certains!)


diff entre kill(pas encore fait d'ailleurs), squit, quit, die
kill == to make quit an user if person is operator ?

proterge tousles send -1 du client message


.o a suppr pour le repo